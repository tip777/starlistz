= render :partial => "searches/search_header"

.l__search__tab__box
  - if @search_user != nil and not @search_user.empty?
    = render "users/user", users: @user_pages
  -if @user_pages != nil
    #user_paginator
      = paginate @user_pages, param_name: 'user_page'