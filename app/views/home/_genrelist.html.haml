// -binding.pry
// = search_form_for @q do |f|
//   %p
//     = f.label :name_cont, '名前'
//     = f.search_field :name_cont
//   = f.submit
// = form_tag(url_for(:action => 'genre'), :remote => true) do
//   = text_field_tag :search
//   = submit_tag "検索"

%ul.genre-list
  - if tag then
    - tag.each do |tag|
      %li
        .title
          = link_to tag.name, chart_path(genre: tag.name, sort: Constants::SORT)
        .number
          = tag.taggings_count

  